var payRate = 17.30;
var hoursWorked = 45;
var overTimePay = 1.5;
var regularTotalPay = payRate * 40


if ( hoursWorked > 40 ) {
var overTimeHours = hoursWorked - 40;
var totalOverTimePay = (payRate * overTimePay) * overTimeHours
var totalGross =  regularTotal + totalOverTimePay;
}


console.log (totalGross)